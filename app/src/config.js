const localhost = '';

const user = {
    /* 用户登陆接口 */login: localhost + '',
    /* 用户登陆界面初始化数据 */getData: localhost + ''
};
const course = {
    /* 课程列表 */list: localhost + ''
};

export default {
    user, course
};
